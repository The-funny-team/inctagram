import { getRootLayout } from '@/shared/ui'

const CreateNewPassword = () => {
  return <h1>Forgot Password with code in uri from server</h1>
}

CreateNewPassword.getLayout = getRootLayout
export default CreateNewPassword
